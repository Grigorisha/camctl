# Архитектура: компоненты

> Статус: черновик
> Обновлён: 2026-06-26
> Автор:

Детальное описание компонентов системы. На каждый компонент — назначение,
интерфейсы, зависимости, заметки. Шаблон ниже копируйте для новых компонентов.

> Релиз пишется на **C++** ([ADR-0003](decisions/0003-service-language-cpp.md)).
> Python-код (`viewer_qt.py`, `guide_thermal.py`, `Test.py`) — прототип для проверки
> камер; служит опорой по логике, но переносится на C++, а не входит в релиз.

## Шаблон описания компонента

### `<имя компонента>`

- **Назначение:** за что отвечает.
- **Входы:** что принимает (данные, события, конфигурация).
- **Выходы:** что отдаёт.
- **Зависимости:** от каких компонентов/библиотек/SDK зависит.
- **Состояние/lifecycle:** как создаётся, работает, освобождается.
- **Заметки/риски:** нюансы, ограничения, TODO.

---

## Компоненты

### Драйвер RGB-камеры (Daheng / gxipy)

- **Назначение:** C++-обёртка над нативной `libgxiapi` для управления камерами Daheng.
- **Входы:** команды (открыть, настроить, старт/стоп), параметры (экспозиция, gain…).
- **Выходы:** сырые/декодированные кадры RGB.
- **Зависимости:** `libgxiapi` (Galaxy SDK), см. [../04-hardware/daheng-me2p.md](../04-hardware/daheng-me2p.md).
- **Заметки:** Bayer-формат зависит от модели (ME2P → GB8, NS-301 → RG8) — выбирать
  поддерживаемый из `PixelFormat`. Прототип логики — `viewer_qt.py`/`Test.py` (Python).

### Драйвер тепловой камеры (USB3.0 SC-SDK / plug617)

- **Назначение:** обёртка над SC-SDK для теплового потока.
- **Зависимости:** SC-SDK, см. [../04-hardware/thermal-usb3-scsdk.md](../04-hardware/thermal-usb3-scsdk.md).
- **Заметки:** ...

### Ядро захвата (capture core)

- **Назначение:** единый интерфейс над драйверами, управление потоками и буферами.
- ...

### Модуль обработки

- **Назначение:** преобразование форматов, совмещение RGB/тепла, ROI, метрики.
- ...

### Интерфейсный слой (API/CLI)

- **Назначение:** выдача кадров и управление сервисом извне.
- **Связи:** [../03-api/overview.md](../03-api/overview.md).
- ...

## Связанные документы

- Обзор архитектуры: [overview.md](overview.md)
- Решения: [decisions/README.md](decisions/README.md)
